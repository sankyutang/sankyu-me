---
import Base from '@/layouts/Base.astro'
import { createReader } from '@keystatic/core/reader'
import keystaticConfig from '../../keystatic.config'
import { renderMarkdocToHtml } from '@/lib/markdoc'

const reader = createReader(process.cwd(), keystaticConfig)

let page: any = null
try {
  page = await reader.collections.pages.read('about')
} catch {}

const content = page ? renderMarkdocToHtml((await page.content()).node) : null
---

<Base title="About" description={page?.seoDescription || 'About me.'}>
  <div class="container mx-auto max-w-3xl px-4 py-12">
    <h1 class="text-3xl font-semibold tracking-tight mb-8">About</h1>
    {content ? (
      <div class="prose prose-neutral max-w-none dark:prose-invert" set:html={content} />
    ) : (
      <p class="text-muted-foreground">内容即将更新</p>
    )}
  </div>
</Base>
